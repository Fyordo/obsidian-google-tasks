{
  "name": "obsidian-google-tasks",
  "version": "0.0.1",
  "description": "Obsidian plugin to integrate with Google Tasks.",
  "main": "main.js",
  "scripts": {
    "dev": "esbuild main.ts --bundle --outfile=main.js --format=cjs --platform=node --external:obsidian --watch",
    "build": "esbuild main.ts --bundle --minify --sourcemap --outfile=main.js --format=cjs --platform=node --external:obsidian",
    "version": "node version-bump.mjs"
  },
  "keywords": [
    "obsidian-plugin",
    "google-tasks"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
